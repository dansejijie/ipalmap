<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="huitumap.backustech.com.huitu_ipalmap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.backustech.ipalmap.IpalmapApplication">
        <activity android:name="com.backustech.ipalmap.activity.DemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.backustech.ipalmap.activity.IpalmapActivity"
            android:theme="@style/IpalmapHeaderBar" android:screenOrientation="portrait">
        </activity>

        <service android:name="com.palmaplus.nagrand.position.ble.BeaconService"/>

    </application>

</manifest>